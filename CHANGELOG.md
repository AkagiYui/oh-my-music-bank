# 更新记录

## 未发布

### 新增

- B 站导入裁剪器新增完整时间轴的振幅波形，按播放器缓冲时间段逐步绘制，支持跳播后的稀疏波形与换源清理；保留试听、点击跳播和起止点拖动，波形不可用时不阻断播放与裁剪。
- 搜索详情与曲目管理统一使用底部全局播放器，切换页面、分页或收起详情不中断播放；支持进度、音量、静音、音质续播及响度均衡，记住播放偏好并适配手机布局。
- B 站导入保留独立裁剪播放器，播放和试听片段会暂停全局播放，恢复全局播放也会暂停裁剪；切音质等待元数据期间同样遵循互斥，避免迟到请求恢复旧播放。

- 哔哩哔哩集成改为 App 扫码登录，支持多个 B 站账号、默认账号切换、账号检查/移除及手动和后台 Cookie 刷新；二维码在浏览器本地生成，凭据仅在服务端存储。
- 导入页可选择账号，收藏夹、视频解析、音频预览、识曲与后台任务使用同一账号；媒体令牌和音频缓存按账号隔离，已提交任务不受默认账号切换影响。
- goose 迁移保留旧 Cookie 和原有任务的账号引用；旧 Cookie 缺少刷新令牌时明确提示重新扫码。刷新先落库新凭据再确认旧凭据失效，失败确认可在重启后重试。

- 将音乐业务拆分为独立的「曲库管理」（`/music`），包含曲目、艺术家、专辑、音频上传、哔哩哔哩导入和收录任务；「系统管理」（`/admin`）保留用户、API Key、日志、站点设置与集成凭据。两区各自提供导航和概览，沿用管理员权限与数据刷新逻辑，旧业务地址自动跳转并保留查询参数和锚点。

- 默认品牌调整为「声迹」，使用面向听众的站点介绍、首页标题与描述和页脚文案；新站点默认生效，升级仅替换整套未定制的旧文案，保留管理员自定义内容。

- 管理员可配置系统标题、站点描述、首页标题与描述、Logo、浏览器图标、页脚文字及链接；后台提供草稿预览、撤销和保存状态，保存后全站导航、页面元数据与文案即时同步。
- 支持开放 API 独立域名，留空由浏览器采用当前来源；首页示例、真实搜索和签名音频地址使用相同 API 来源，登录及管理接口保持同源。
- 品牌配置使用 goose 迁移与事务保存，统一限制文案长度、URL 协议和格式；公开 DTO 隔离管理字段和凭据，失败不产生部分保存。关闭注册同步隐藏入口并阻止注册页面展示表单。
- 新增维护命令 `ommb reset-password`，支持按邮箱重置密码、隐藏终端输入或从标准输入读取密码，并在同一事务中撤销账号的全部登录会话。两个容器镜像均内置该命令，CI 增加 CLI 构建检查。

### 优化与修复

- 设置 API 改为完整 PUT，`brandName` 替换为 `systemTitle`，日志保留天数统一为数字；不保留旧字段兼容层。已有品牌通过一次性数据迁移保留。
- 设置缓存写入与刷新串行化，事务按固定键顺序获取行锁，避免旧快照覆盖新值及交叉更新死锁。
- 将 shadcn 基础组件迁移至 Base UI，应用 `b1Zh6udKE` 的 Lyra 样式、蓝色主题及本地 Noto Sans 字体；阻止前端直接导入 Radix。
- 错误反馈统一使用 Sonner 浮层，支持重复错误合并、长内容滚动和关闭，避免错误出现或消失时挤动页面布局。
- 补齐导航、列表、编辑区、封面、播放器和裁剪器的 Lyra 直角样式；艺术家头像使用官方 Avatar 并保留圆形设计。
- API Key 启用状态、曲目可搜索状态、音质和 HTTP 方法标签使用描边 Badge；启用状态改为“已启用”，并为正向状态添加勾选图标，与主色操作按钮区分。
- 修正 Base UI 按钮迁移后的显式表单提交，以及任务进度的视觉与无障碍数值同步。
- 后台任务队列为空时不再重复打印 `record not found`，保留任务行锁、领取条件与排序。

### 已知限制

- 波形目前独立请求音频 Range，并按 128 KiB 块读取；绘图范围受播放器缓冲时间段限制，但字节读取不保证复用播放器缓存，也可能读取未缓冲内容或累计覆盖整文件。24 秒、293,103 字节的分片 AAC 样本在仅将 0–5 秒和 18–21 秒标为已缓冲时，波形请求已覆盖整文件；严格仅消费播放链路已取得数据的方案尚未实现。

### 验证

- 波形通过前端格式/lint/类型检查、31 项单元测试、58 项 Chromium E2E 及生产构建，后端 CORS 调整通过 Go vet 和服务端构建；E2E 使用模拟 API、可控缓冲时间段与真实分片 MP4/AAC 解码，覆盖稀疏和完整波形、静音、切分 P、裁剪交互、窄屏布局及 Range 不可用时的降级。未验证真实 B 站 CDN 和播放器缓存复用；测试通过不代表已解决上述读取限制。
- 全局播放器通过前端格式/lint/类型检查、26 项单元测试、55 项 Chromium E2E 及生产构建；E2E 使用模拟 API 与支持 Range 的真实 WAV，覆盖跨页持续播放、音质续播、裁剪双向互斥、退出清理和 1280/390/320px 下的内容与通知避让。
- 哔哩哔哩账号改造通过隔离 PostgreSQL 上的 Go 全量 race、vet、服务端构建，以及前端检查、23 项单元测试、47 项 Chromium E2E（模拟 API）和生产构建；真实 B 站接口已验证二维码生成与未扫码轮询，已登录 Cookie 轮换通过协议模拟验证，仍需实际扫码账号确认。
- 管理拆分通过前端格式/lint/类型检查、23 项单元测试、42 项 Chromium E2E（模拟 API）及生产构建；覆盖分区导航、新旧路由、权限隔离、业务编辑、缓存刷新和移动端布局。
- 前端锁定依赖安装、格式/lint/类型检查、17 项单元测试、28 项 Chromium E2E 和生产构建通过。
- 独立 PostgreSQL 实例上的 Go 全量 race 测试、vet、服务端及 CLI 构建通过；覆盖品牌持久化、空值清除、非法 URL、完整请求校验、权限、失败回滚和迁移往返。
- 浏览器回归覆盖保存即时同步、刷新恢复、可选配置清空、错误保留草稿、独立 API 请求及音频来源、关闭注册、配置加载失败重试和纯文本安全渲染；同时检查桌面与手机布局。
