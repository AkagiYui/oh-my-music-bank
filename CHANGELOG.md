# 更新记录

## 未发布

### 新增

- 默认品牌调整为「声迹」，使用面向听众的站点介绍、首页标题与描述和页脚文案；新站点默认生效，升级仅替换整套未定制的旧文案，保留管理员自定义内容。

- 管理员可配置系统标题、站点描述、首页标题与描述、Logo、浏览器图标、页脚文字及链接；后台提供草稿预览、撤销和保存状态，保存后全站导航、页面元数据与文案即时同步。
- 支持开放 API 独立域名，留空由浏览器采用当前来源；首页示例、真实试搜和签名音频地址使用相同 API 来源，登录及管理接口保持同源。
- 品牌配置使用 goose 迁移与事务保存，统一限制文案长度、URL 协议和格式；公开 DTO 隔离管理字段和凭据，失败不产生部分保存。关闭注册同步隐藏入口并阻止注册页面展示表单。
- 新增维护命令 `ommb reset-password`，支持按邮箱重置密码、隐藏终端输入或从标准输入读取密码，并在同一事务中撤销账号的全部登录会话。两个容器镜像均内置该命令，CI 增加 CLI 构建检查。

### 优化与修复

- 设置 API 改为完整 PUT，`brandName` 替换为 `systemTitle`，日志保留天数统一为数字；不保留旧字段兼容层。已有品牌通过一次性数据迁移保留。
- 设置缓存写入与刷新串行化，事务按固定键顺序获取行锁，避免旧快照覆盖新值及交叉更新死锁。
- 将 shadcn 基础组件迁移至 Base UI，应用 `b1Zh6udKE` 的 Lyra 样式、蓝色主题及本地 Noto Sans 字体；阻止前端直接导入 Radix。
- 错误反馈统一使用 Sonner 浮层，支持重复错误合并、长内容滚动和关闭，避免错误出现或消失时挤动页面布局。
- 补齐导航、列表、编辑区、封面、播放器和裁剪器的 Lyra 直角样式；艺术家头像使用官方 Avatar 并保留圆形设计。
- API Key 启用状态、曲目可搜索状态、音质和 HTTP 方法标签使用描边 Badge；启用状态改为“已启用”，并为正向状态添加勾选图标，与主色操作按钮区分。
- 修正 Base UI 按钮迁移后的显式表单提交，以及任务进度的视觉与无障碍数值同步。
- 后台任务队列为空时不再重复打印 `record not found`，保留任务行锁、领取条件与排序。

### 验证

- 前端锁定依赖安装、格式/lint/类型检查、17 项单元测试、28 项 Chromium E2E 和生产构建通过。
- 独立 PostgreSQL 实例上的 Go 全量 race 测试、vet、服务端及 CLI 构建通过；覆盖品牌持久化、空值清除、非法 URL、完整请求校验、权限、失败回滚和迁移往返。
- 浏览器回归覆盖保存即时同步、刷新恢复、可选配置清空、错误保留草稿、独立 API 请求及音频来源、关闭注册、配置加载失败重试和纯文本安全渲染；同时检查桌面与手机布局。
